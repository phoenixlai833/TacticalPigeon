module.exports = {
  secret: 'secret', // we'll change this (it's a secret and should not be published on a public GitHub repository
  resave: false,
  saveUninitialized: true,
  cookie: {secure: false}
};